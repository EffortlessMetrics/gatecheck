# Requirements

- Evaluate ordered gate policy against a frozen evidence snapshot.
- Keep evaluation pure and deterministic.
- Support artifact, receipt, CI, issue-link, review, and attestation requirements.
- Emit stable JSON and Markdown outputs.
- Keep adapters outside the pure evaluator.
